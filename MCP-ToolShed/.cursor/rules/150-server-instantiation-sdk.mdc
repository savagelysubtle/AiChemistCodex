---
description:
globs: **/*.ts
alwaysApply: false
---
// Example Server Setup (typically in index.ts or main.ts)
import { createServer } from "@modelcontextprotocol/sdk";
import { MyResource } from "./resources/my-resource";
import { MyTool } from "./tools/my-tool";

const server = createServer({
    resources: [new MyResource()],
    tools: [new MyTool()],
});

server.listen(); // Listens on stdio by default